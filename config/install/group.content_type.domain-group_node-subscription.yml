langcode: en
status: true
dependencies:
  config:
  - group.type.domain
  - node.type.subscription
  module:
  - gnode
  - node
id: domain-group_node-subscription
label: 'Domain: Group node (Subscription)'
description: Adds <em class="placeholder">Subscription</em> content to groups both
  publicly and privately.
group_type: domain
content_plugin: group_node:subscription
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
