langcode: en
status: true
dependencies:
  config:
  - group.type.domain
  - node.type.plan
  module:
  - gnode
  - node
id: domain-group_node-plan
label: 'Domain: Group node (Billing Plan)'
description: Adds <em class="placeholder">Billing Plan</em> content to groups both
  publicly and privately.
group_type: domain
content_plugin: group_node:plan
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
