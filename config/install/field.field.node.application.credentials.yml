langcode: en
status: true
dependencies:
  config:
  - field.storage.node.credentials
  - node.type.application
  module:
  - jsonb
id: node.application.credentials
field_name: credentials
entity_type: node
bundle: application
label: Credentials
description: A key:value literal describing a credential object. A "type" key is used
  to interpret the strategy with metadata. Supported types are apikey, oauth, oauth2,
  http_basic and token. Please view a detailed reference.
required: false
translatable: false
default_value: {}
default_value_callback: ""
settings: {}
field_type: json
