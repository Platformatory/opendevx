langcode: en
status: true
dependencies:
  config:
  - group.type.domain
  - node.type.ticket
  module:
  - gnode
  - node
id: domain-group_node-ticket
label: 'Domain: Group node (Ticket)'
description: Adds <em class="placeholder">Ticket</em> content to groups both publicly
  and privately.
group_type: domain
content_plugin: group_node:ticket
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
