langcode: en
status: true
dependencies:
  config:
    - group.type.domain
    - node.type.repository
  module:
    - gnode
    - node
id: domain-group_node-repository
label: 'Domain: Group node (Repository)'
description: 'Adds <em class="placeholder">Repository</em> content to groups both publicly and privately.'
group_type: domain
content_plugin: 'group_node:repository'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
