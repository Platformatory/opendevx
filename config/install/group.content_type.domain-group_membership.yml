langcode: en
status: true
dependencies:
  config:
    - group.type.domain
  module:
    - user
id: domain-group_membership
label: 'Domain: Group membership'
description: 'Adds users to groups as members.'
group_type: domain
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
