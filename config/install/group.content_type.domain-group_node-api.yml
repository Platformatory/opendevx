langcode: en
status: true
dependencies:
  config:
    - group.type.domain
    - node.type.api
  module:
    - gnode
    - node
id: domain-group_node-api
label: 'Domain: Group node (API)'
description: 'Adds <em class="placeholder">API</em> content to groups both publicly and privately.'
group_type: domain
content_plugin: 'group_node:api'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
