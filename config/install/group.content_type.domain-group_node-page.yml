langcode: en
status: true
dependencies:
  config:
    - group.type.domain
    - node.type.page
  module:
    - gnode
    - node
id: domain-group_node-page
label: 'Domain: Group node (Page)'
description: 'Adds <em class="placeholder">Page</em> content to groups both publicly and privately.'
group_type: domain
content_plugin: 'group_node:page'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
